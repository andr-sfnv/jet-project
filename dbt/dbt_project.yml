name: 'xkcd_analytics'
version: '1.0.0'

profile: 'xkcd_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  xkcd_analytics:
    staging:
      +materialized: view
      +schema: "{{ target.schema }}_staging"
    marts:
      +materialized: table
      +schema: "{{ target.schema }}_marts"
